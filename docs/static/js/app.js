// Read the JSON file
d3.json("samples.json").then((data) => {
    console.log(data);
    //  Create the Traces
    // var trace1 = {
    //   x: data.organ,
    //   y: data.survival.map(val => Math.sqrt(val)),
    //   type: "bar",
    //   name: "Biodiversity",
    //   orientation: "h"
});